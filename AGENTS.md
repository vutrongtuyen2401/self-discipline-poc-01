# Quy định hệ thống

## Ngôn ngữ phản hồi
- **Nguyên tắc vĩnh viễn**: Mọi câu trả lời, giải thích, thông báo tiến độ và trao đổi với người dùng **bắt buộc phải là tiếng Việt 100%** (các thuật ngữ kỹ thuật, tên mã nguồn, file path, symbol, log, command giữ nguyên tính chính xác).

## Đồng bộ mã nguồn (Git Push)
- **Quy tắc bắt buộc**: Sau mỗi khi hoàn thành code, test và commit git, AI agent **bắt buộc phải tự động đẩy (push) mã nguồn lên GitHub** (`git push origin main` hoặc branch hiện tại) mà không cần chờ người dùng nhắc nhở.

